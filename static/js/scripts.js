/**
 * Shared UI enhancement script.
 * Initialize and enable mobile menu functionality.
 */

document.addEventListener("DOMContentLoaded", () => {
    // Mobile menu is now enabled and working
    // No elements are hidden
});
